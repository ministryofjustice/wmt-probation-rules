module.export = function () {
  
}

// COUNT (case details)
// WHERE
// Location == ‘custody’ AND
// TierCode == empty string OR TierCode == zero
// AND RowType == (O, W or U depending on postfix)

// COUNT (case details)
// WHERE
// Location == ‘custody’ AND
// TierCode == [1 - 7] AND
// RowType == (O, W or U depending on postfix)