const TierTotals = require('../../../app/points/domain/tier-totals')
const calculatePointsForTier = require('../../../app/points/calculate-points-for-tier')
describe('points/calculate-points-for-tier', function() {
    var tierPoints = new TierTotals(10, 0, 0, 0)
    var tierPoints = 10
    var tierPoints = 10
    var tier = calculatePointsForTier
})
