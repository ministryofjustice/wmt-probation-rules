const expect = require('chai').expect
const Tier = require('../../../../app/points/domain/Tier')

describe('', function () {
  it('', function () {
    var tier = new Tier()
    expect(tier).to.not.be.NaN
  })
})
